{
  "name": "bfr-canvas-app-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright automation for the BFR Power Apps canvas app",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^20.14.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.5.4"
  }
}
